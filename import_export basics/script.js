// Library are used to give features[particular features we can customize]
// -GSAP(moveable animation)
// -LENIS(smooth scrolling)
// -ReactJS(make ui in website)

// Framework[get all in one not much customization]
// -NEXTJS
// -ANGULARJS



// import chacha from './app.js';
// console.log(chacha);


// import data, { user } from './app.js';

// console.log(data,user);

import name, { age, skills } from './app.js';
import user2 from './test.js';
console.log(name,user2);
console.log(age,skills);